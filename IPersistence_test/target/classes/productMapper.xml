<mapper namespace="product">
<!--    sql语句-->
    <select id="selectList">
        select * from product
    </select>

    <select id="selectOne">
        select * from product where id=?
    </select>
</mapper>